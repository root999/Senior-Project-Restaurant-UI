@model IEnumerable<WebApplication5.Models.MyapiOrder>

@{
    ViewData["Title"] = "Index";
}

<h1>Siparişler</h1>

@*<p>
    <a asp-action="Create">Create New</a>
</p>*@
<table class="table">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.IssueTime)*@
                Sipariş Veriliş Tarihi
            </th>

            <th>
                @*@Html.DisplayNameFor(model => model.PlannedTime)*@
                Sipariş Tarihi
            </th>

            <th>
                @*@Html.DisplayNameFor(model => model.Customer)*@
                Müşteri
            </th>
            <th>Sipariş Listesi</th>
            <th></th>
           
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IssueTime)
            </td>
          
            <td>
                @Html.DisplayFor(modelItem => item.PlannedTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.Name)
                @Html.DisplayFor(modelItem => item.Customer.Surname)
            </td>
       
            <td>
                @foreach( var product in item.MyapiProductsinorders)
                {
                    @Html.DisplayFor(productName => product.Product.Name)
                    <br />
                }
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |*@
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> 
                @*<a asp-action="Delete" asp-route-id="@item.Id">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
